
[tool.setuptools]
py-modules = []

[project]
dependencies = [
    "llama-cpp-python",
    "numpy",
    "diffusers",
    "transformers",
    "sentencepiece",
    "torch",
    "wheel",
    "ninja",
    "xformers",
]

name            = "nnll"
version         = "0.0.0"
description     = "Experimental ML modules library"
authors         = [{ name = "darkshapes" }]
requires-python = ">= 3.12"
